# my zsh custom config
First things first, ZSH must be installed in your system

Clone this repo to `$HOME/.config`

Then place `.zshenv` at your `$HOME` folder.

Set `ZSH` as default shell: `chsh -s /bin/zsh`
